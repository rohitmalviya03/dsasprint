{
  "name": "dsasprint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "postinstall": "npm run setup",
    "dev": "node scripts/dev.js",
    "start": "node server/src/server.js",
    "build": "npm run build --prefix client && node scripts/copy-static.js"
  }
}
